/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseDto } from '../../models/response-dto';

export interface ApiEventParticipantsGet$Plain$Params {
  EventId?: number;
  EmployeeId?: number;
  PageNumber?: number;
  PageSize?: number;
}

export function apiEventParticipantsGet$Plain(http: HttpClient, rootUrl: string, params?: ApiEventParticipantsGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseDto>> {
  const rb = new RequestBuilder(rootUrl, apiEventParticipantsGet$Plain.PATH, 'get');
  if (params) {
    rb.query('EventId', params.EventId, {});
    rb.query('EmployeeId', params.EmployeeId, {});
    rb.query('PageNumber', params.PageNumber, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseDto>;
    })
  );
}

apiEventParticipantsGet$Plain.PATH = '/api/EventParticipants';

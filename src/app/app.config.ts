import { ApplicationConfig, provideZoneChangeDetection, importProvidersFrom } from '@angular/core';
import { provideApiConfiguration } from './services/api-services/api-configuration';
import { environment } from '../environments/environment';
import { provideRouter, withComponentInputBinding, withRouterConfig } from '@angular/router';
import { provideHttpClient, withInterceptors } from '@angular/common/http';
import { tokenInterceptor } from './core/interceptors/token.interceptor';
import { 
  LucideAngularModule, 
  LayoutDashboard, 
  CalendarCheck, 
  FileText, 
  Users, 
  Wallet, 
  LogOut, 
  Search, 
  Bell, 
  ChevronDown,
  Clock,
  Calendar,
  CheckCircle2,
  TrendingUp,
  Filter,
  Download,
  ChevronLeft,
  ChevronRight,
  Send,
  Info,
  Grid,
  List,
  Mail,
  Phone,
  ExternalLink,
  CreditCard,
  Receipt,
  CheckSquare,
  ShieldCheck,
  Check,
  X,
  User,
  MoreVertical,
  MoreHorizontal,
  Plus,
  Edit2,
  Trash2,
  UserPlus,
  Eye,
  Briefcase,
  History,
  ArrowRight,
  MapPin,
  Menu,
  Lock,
  EyeOff,
  AlertCircle,
  Settings,
  Shield,
  Palette,
  Smartphone,
  HelpCircle,
  Globe,
  Sun,
  Moon,
  Monitor,
  DollarSign,
  Building,
  Landmark,
  Hash,
  ArrowLeftRight,
  Save,
  Loader2,
  Fingerprint,
  BookOpen,
  Leaf,
  Droplets,
  CalendarDays,
  Bot,
  Square,
  Sparkles
} from 'lucide-angular';

import { routes } from './app.routes';

export const appConfig: ApplicationConfig = {
  providers: [
    provideZoneChangeDetection({ eventCoalescing: true }), 
    provideRouter(
      routes, 
      withComponentInputBinding(), 
      withRouterConfig({ onSameUrlNavigation: 'reload' })
    ),
    provideHttpClient(withInterceptors([tokenInterceptor])),
    provideApiConfiguration(environment.apiUrl),
    importProvidersFrom(
      LucideAngularModule.pick({
        LayoutDashboard, 
        CalendarCheck, 
        FileText, 
        Users, 
        Wallet, 
        LogOut, 
        Search, 
        Bell, 
        ChevronDown,
        Clock,
        Calendar,
        CheckCircle2,
        TrendingUp,
        Filter,
        Download,
        ChevronLeft,
        ChevronRight,
        Send,
        Info,
        Grid,
        List,
        Mail,
        Phone,
        ExternalLink,
        CreditCard,
        Receipt,
        CheckSquare,
        ShieldCheck,
        Check,
        X,
        User,
        MoreVertical,
        MoreHorizontal,
        Plus,
        Edit2,
        Trash2,
        UserPlus,
        Eye,
        Briefcase,
        History,
        ArrowRight,
        MapPin,
        Menu,
        Lock,
        EyeOff,
        AlertCircle,
        Settings,
        Shield,
        Palette,
        Smartphone,
        HelpCircle,
        Globe,
        Sun,
        Moon,
        Monitor,
        DollarSign,
        Building,
        Landmark,
        Hash,
        ArrowLeftRight,
        Save,
        Loader2,
        Fingerprint,
        BookOpen,
  Leaf,
  Droplets,
  CalendarDays,
  Bot,
  Square,
  Sparkles
      })
    )
  ]
};

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseDto } from '../../models/response-dto';

export interface ApiEmployeesGet$Json$Params {
  FullName?: string;
  Email?: string;
  SearchTerm?: string;
  DepartmentId?: number;
  PageNumber?: number;
  PageSize?: number;
}

export function apiEmployeesGet$Json(http: HttpClient, rootUrl: string, params?: ApiEmployeesGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseDto>> {
  const rb = new RequestBuilder(rootUrl, apiEmployeesGet$Json.PATH, 'get');
  if (params) {
    rb.query('FullName', params.FullName, {});
    rb.query('Email', params.Email, {});
    rb.query('SearchTerm', params.SearchTerm, {});
    rb.query('DepartmentId', params.DepartmentId, {});
    rb.query('PageNumber', params.PageNumber, {});
    rb.query('PageSize', params.PageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseDto>;
    })
  );
}

apiEmployeesGet$Json.PATH = '/api/Employees';
